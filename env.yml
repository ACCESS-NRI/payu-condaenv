name: payu
channels:
  - accessnri
  - conda-forge
dependencies:
  - python==3.10
  # note that the deployment requires a specific version of 'accessnri::payu' to run
  - accessnri::payu==1.1.4
  - f90nml
  - conda-lock
  - conda-pack
  - gh
  - git
  - nco
  - pytest
  - openssh>=8.3
